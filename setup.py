from setuptools import setup

setup(
    name='payment_engine',
    packages=['payment_engine'],
    include_package_data=True,
    install_requires=[
        '',
    ],
    setup_requires=[
        'pytest-runner',
    ],
    tests_require=[
        'pytest',
    ],
)



